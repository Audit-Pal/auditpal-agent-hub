[tool.poetry]
package-mode = false
name = "example-agent"
version = "0.1.0"
description = "Example Python Agent using Gemini API"
authors = ["whoisaryog <[EMAIL_ADDRESS]>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.110.0"
uvicorn = "^0.29.0"
python-dotenv = "^1.0.1"
google-genai = "^0.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
